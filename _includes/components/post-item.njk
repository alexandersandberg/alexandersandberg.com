{% macro postItem(type, item) %}
	<article class="separator-xl">
		<a href="/{{ pages[type].path }}{{ item.slug }}/">
			<h2>{{ item.title }}</h2>
			<p>{{ item.excerpt }}</p>
		</a>
	</article>
{% endmacro %}